---
create_users:
  - username: vinodh
    groups: sysadmin
    servers:
      - slave1
  - username: shahan
    groups: [dbadmin, networkadmin]
    servers:
      - ansible2
      - slave1

delete_users:
  - username: shahan
    servers:
      - slave1
