sudofilecontent:
  sysadmin:
    commands:
      - ALL
    servers:
      - ALL

  dbadmin:
    commands:
      - /sbin/useradd
      - /sbin/reboot
    servers:
      - controller
      - slave1
      - ansible2
  networkadmin:
    commands:
      - /sbin/userdel
    servers:
      - slave1
